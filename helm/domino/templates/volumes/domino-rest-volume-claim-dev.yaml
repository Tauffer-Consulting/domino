apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: domino-rest-volume-claim-dev
spec:
  volumeName: domino-rest-volume-dev
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
